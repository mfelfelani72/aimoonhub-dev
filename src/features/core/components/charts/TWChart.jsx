import React from "react";
import { TEChart } from "tw-elements-react";

const TWChart = ({ type, data, options }) => {
  return <TEChart type={type} data={data} options={options} />;
};
export default TWChart;
